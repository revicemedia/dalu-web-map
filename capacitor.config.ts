import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dalu.app",
  appName: "DaLu Services",
  webDir: "build",
  // webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
